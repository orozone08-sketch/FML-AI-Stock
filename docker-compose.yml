services:
  web:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - uploads:/app/uploads
    networks:
      - shared-mysql

networks:
  shared-mysql:
    external: true
    name: shared-mysql

volumes:
  uploads:
